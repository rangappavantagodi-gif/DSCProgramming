Find the Average of Elements in an Array — C Program

This program calculates the average value of all elements entered by the user in an array. It uses basic concepts such as loops, arrays, and arithmetic operations in C.

📌 Features

Accepts the number of array elements from the user

Reads all elements one by one

Calculates the total sum

Computes the average

Displays the result with two decimal precision

📂 How It Works

The user enters the number of elements (n)

The program takes n integer inputs and stores them in an array

Each element is added to a running sum

The average is computed using the formula:

average = sum/n

The final average is displayed to the user

🖥️ Sample Output
--Program to find the average of elements in array--
Enter the number of elements in array:
5
Enter 5 elements:
10
20
30
40
50
Average of array elements = 30.00

🚀 How to Run

Save the code in a file such as average_array.c

Compile the program:

gcc average_array.c -o average_array


Run the compiled program:

./average_array