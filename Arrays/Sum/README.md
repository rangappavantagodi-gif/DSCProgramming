Sum of All Elements in an Array — C Program

This program calculates the sum of all elements in an integer array entered by the user.
It demonstrates the basic use of arrays, loops, and user input handling in C.

📌 Features

Accepts the number of elements from the user

Takes array elements as input

Computes the total sum

Displays the final result

📂 How It Works

The user enters the number of elements (n)

The program collects n integer values

Each value is added to a running total (sum)

The final sum is displayed to the user

🖥️ Sample Output
--Program to find the sum of all elements in array--
Enter the number of elements in array:
5
Enter 5 elements:
10
20
30
40
50
Sum of array elements = 150

🚀 How to Run

Save the file, for example: sum_array.c

Compile using a C compiler:

gcc sum_array.c -o sum_array


Run the output file:

./sum_array